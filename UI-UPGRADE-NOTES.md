# ai-chat UI 升级 — 交付说明

一次**兼容性 UI 升级**：把 ai-chat 优化为干净、专业、轻量、桌面优先 / 移动可用的"私人 ChatGPT 网页版"，风格向主网站靠拢。**未恢复任何已砍功能，未改动 Provider / 模型 / API Key 配置。**

## 1. 完成状态
已完成。前端视觉 + 交互重做、死代码物理清除、设置弹窗（含最小后端）均已实现，并通过无头浏览器端到端冒烟 + ESLint + 语法校验。

## 2. 修改文件
| 文件 | 目的 |
|---|---|
| `public/style.css` | 末尾追加"UI UPGRADE OVERRIDES"块：中性灰 + 赭石（`#C2683F`）主题令牌、默认浅色、去赛博装饰、消息去气泡化为文档流、代码块统一、输入区/侧栏/空状态、**设置弹窗样式**、移动端无横向溢出 + 安全区。原有 1.4 万行未改，靠层叠顺序覆盖，便于回滚。 |
| `public/app.js` | 默认主题改浅色；**物理删除** Finder/远程控制/终端/文件传输死代码（-1447 行）并清理 `setActiveTab`/`initControlPanel`/登出/`focusMacOSPrimaryField` 中的相关引用；隐藏但保留 `#modelSelect`；流式"用户上滚不强拉回底部"；**设置弹窗逻辑** + 调用 `PATCH /api/auth/profile`。 |
| `public/index.html` | 隐藏模型选择器/移动模型 pill/输入助手/联网按钮；登录页与空状态文案去"远程控制/多模型/设备控制"；侧栏新增设置齿轮 + 移动"更多"菜单新增"设置"；**新增设置弹窗 DOM**；资源 `?v=` 版本号刷新。 |
| `server.js` | 新增 `PATCH /api/auth/profile`（鉴权 + 限流）：校验当前密码后改用户名/密码，bcrypt 加密，改名后重签 token。 |
| `db.js` | 新增 `userQueries.findWithPasswordById` / `updateUsername` / `updatePassword`。 |

净变化：5 文件，**+565 / −1510 行**（app.js 4510 → 3151）。

## 3. 新增文件
- `UI-UPGRADE-NOTES.md`（本文件）。

## 4. 删除文件
- 服务器专用分支后续清理了 Mac 控制、Finder、ngrok 运维和禁用记忆实现；核心聊天路由与 Provider 保留。

## 5. 已实现
- 中性灰 + 克制赭石点缀的统一主题，**默认浅色**，浅/深/跟随系统可切换。
- 消息改为文档流阅读体验（弱气泡、Markdown 标题/段落/列表/引用间距优化）。
- 代码块：语言名 + 复制按钮 + 深色底 + 横向滚动（沿用 marked + highlight）。
- 输入区只保留发送 / 停止；Enter 发送、Shift+Enter 换行、多行自增高（沿用原逻辑）。
- 流式输出时**用户上滚不被强拉回底部**；上滚时显示"回到最新"按钮。
- 空状态简化为"打开即输入"。
- **设置弹窗**：改用户名、改密码（旧/新/确认）、外观切换、退出登录；桌面在侧栏齿轮、移动在"更多"菜单。
- **最小后端** `PATCH /api/auth/profile`：校验当前密码、bcrypt、改名重签 token、绝不回显密码。
- 移动端：无横向溢出、底部安全区内边距、键盘场景沿用原 visualViewport 处理。
- 物理删除 Finder/远程控制/终端/文件传输死代码，app.js 减重约 32%。

## 6. 保留（未破坏）
发送消息、AI **SSE 流式输出**、Markdown 渲染、代码块、代码复制、停止生成、会话历史、新建/删除/重命名会话、登录/注册/管理员、记忆（后端仍接入对话上下文，前端 tab 维持隐藏）、移动端使用。**Provider/模型/API Key 配置未触碰。**

## 7. 未实现 / 刻意不做
- 文件上传、远程控制、Finder、终端、支付、复杂后台、社交、插件市场、多用户权限 —— 按要求不做 / 不恢复。
- 模型选择 UI —— 按要求隐藏（`#modelSelect` 仍在 DOM，空值由服务端解析为默认模型，不暴露 Provider/模型名）。

## 8. 安全影响
- 改用户名/密码**必须校验当前密码**；密码 bcrypt(12) 加密，接口与日志均不回显明文；`findById` 不含 password。
- 当前密码错误返回 **400**（而非 401），以避免前端全局 401→自动登出拦截把用户在输错密码时踢下线；前端额外用 `authRedirect:false` 调用。
- 改用户名后 JWT 内 `username` 失效 → 后端**重签并下发新 token**，前端同步更新 localStorage。
- 未改动鉴权机制、JWT 密钥处理、CORS、限流策略（改密接口复用既有 `authLimiter`）。

## 9. 验证结果
环境无法联网安装依赖（npm 镜像 403），故未在本地编译 better-sqlite3 / 启动真实服务；改用**无头 Chromium 端到端冒烟**（静态托管 `public/` + 拦截 `/api/*`，含模拟 SSE 流）验证前端，并以 ESLint `no-undef` + `node --check` 做静态校验。

冒烟 14 项全部通过：加载进入对话视图 / 默认浅色 / 模型选择器隐藏 / 流式输出 + Markdown / 代码块语言名 + 复制 / 新建会话 / 设置弹窗打开 / 主题切换 / **当前密码错误→内联报错且不登出** / **改资料成功→用户名+token 更新** / 移动端无横向溢出 / 无意外控制台报错。

对照需求 18 项自测：1-11、12-15、17 通过；16（改用户名/密码成功+失败）成功与"当前密码错误"失败均已验证（用户名占用 409 / 弱密码 400 为后端同款校验）；18 未触碰 Provider 配置。**仍需你在服务器上用真实后端 + Provider Key 复核流式真机表现与改密入库。**

## 10. 移动端适配
设置弹窗在窄屏转为底部抽屉（圆角顶 + 安全区）；消息容器 `overflow-x:hidden`，`pre/table/img/代码块` 限宽 100%；输入区底部 `env(safe-area-inset-bottom)`。沿用原有 `#mobileMoreBtn`（⋯）作为移动操作中心，新增"设置"入口。

## 11. 兼容性修正
- 已停止加载不兼容浏览器的 CommonJS `highlight.common.min.js`，消除 `require is not defined` 控制台错误。代码块继续使用应用内置的 JS / Python 基础高亮，并保留语言标签、复制按钮与横向滚动。

## 12. 部署与回滚
- **部署**：拉取服务器专用分支 `server/aichatupdated-20260628` 后重启服务即可。`package.json` 的 `prestart` 会自动 `npm run build`（esbuild+csso）重建 `public/dist/`（dist 已 gitignore，不入库）。`?v=` 已刷新以避免浏览器缓存旧资源。
- **回滚**：全部改动集中在 `server/aichatupdated-20260628` 分支；生产部署前必须创建时间戳备份，出现问题时恢复备份即可。后端仅新增一个端点 + 三条 db query，删除它们即可单独回退而不影响既有登录。CSS 为末尾追加块，删除该块即恢复旧视觉。
